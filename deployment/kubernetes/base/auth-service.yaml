apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: slotify
  labels:
    app: auth-service
spec:
  type: ClusterIP
  selector:
    app: auth-service
  ports:
  - port: 5001
    targetPort: 5001
    protocol: TCP
    name: http
